server:
  host: "0.0.0.0"
  port: 6540
  read_timeout: 30s
  write_timeout: 0s

library:
  path: "../media"
  name: "Медиатека"
  scan_interval: 1h

thumbnails:
  output_dir: "data/thumbnails"
  cache_capacity: 1000
  cache_max_size: 536870912

database:
  path: data/library.db

logging:
  level: info
  pretty: true
